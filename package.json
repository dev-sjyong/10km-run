{
  "name": "10km-run-weather-scheduler",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "cf:kv:create": "wrangler kv namespace create WEATHER_STATE",
    "vapid:generate": "node scripts/generate-vapid-keys.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  }
}
